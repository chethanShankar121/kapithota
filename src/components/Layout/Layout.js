import React from "react";
import './Layout.scss';
import AppHeader from "../SecondaryComponents/AppHeader/AppHeader";

const Layout = props => {
    return (
        <div id="site-layout">
        <AppHeader />
        </div>
    )
}

export default Layout;